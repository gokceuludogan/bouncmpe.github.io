{% extends "_base.md.j2" %}

{% block front_matter -%}
type: "seminar"
datetime: "{{ datetime }}"
speaker: "{{ speaker }}"
topic: "{{ topic }}"
duration: "{{ duration }}"
location: "{{ location }}"
---
{%- endblock %}

{% block content -%}
{% if thumbnail %}![{{ title }}]({{ thumbnail }}){% endif %}
{{ description }}
{%- endblock %}
